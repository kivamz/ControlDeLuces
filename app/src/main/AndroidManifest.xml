<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kmzwebdesign.controldeluces">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MyNavigationDrawerTheme"
        android:allowBackup="true">
        <activity android:name=".MainActivity">
        </activity>

        <activity android:name=".SettingActivity" />

        <activity
            android:name=".ConfiguracionPaso1Activity"
            android:label="@string/title_activity_configuracion_paso1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ConfiguracionPaso2Activity"
            android:label="@string/title_activity_configuracion_paso2">

        </activity>

    </application>

</manifest>